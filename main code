<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Spark a Story</title>
<style>
/* ===== ROOT VARIABLES ===== */
:root {
  --neon-orange: #ff6f00;
  --neon-yellow: #ffeb3b;
  --black: #111;
  --white: #fff;
  --border: 2px solid var(--black);
  --radius: 12px;
  --padding: 1.5rem;
  --max-width: 1200px;
  --transition: 0.3s ease-in-out;
}

/* ===== BASE ===== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Segoe UI", Arial, sans-serif;
}

body {
  background: var(--black);
  color: var(--white);
  line-height: 1.6;
}

a {
  text-decoration: none;
  color: inherit;
}

/* ===== HEADER ===== */
header {
  position: relative;
  background: linear-gradient(135deg, var(--neon-orange), var(--neon-yellow));
  padding: 4rem 2rem;
  text-align: center;
  border-bottom: var(--border);
}
header h1 {
  font-size: 3rem;
  color: var(--black);
  text-shadow: 0 0 10px var(--neon-orange);
}
header p {
  margin-top: 1rem;
  font-size: 1.2rem;
  color: var(--black);
}

/* ===== NAVBAR ===== */
nav {
  position: sticky;
  top: 0;
  display: flex;
  justify-content: center;
  gap: 2rem;
  background: var(--black);
  padding: 1rem 0;
  border-bottom: 3px solid var(--neon-yellow);
  z-index: 1000;
}
nav a {
  color: var(--neon-yellow);
  font-weight: bold;
  padding: 0.5rem 1rem;
  border: var(--border);
  border-radius: var(--radius);
  transition: var(--transition);
}
nav a:hover {
  background: var(--neon-yellow);
  color: var(--black);
  border-color: var(--neon-orange);
}

/* ===== HERO ===== */
.hero {
  position: relative;
  min-height: 80vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
  background: linear-gradient(135deg, #1a1a1a 0%, #222 100%);
  padding: 2rem;
  overflow: hidden;
}
.hero h2 {
  font-size: 2.5rem;
  color: var(--neon-orange);
  text-shadow: 0 0 20px var(--neon-orange);
  margin-bottom: 1rem;
}
.hero p {
  font-size: 1.2rem;
  max-width: 700px;
  color: var(--white);
}

/* ===== SECTIONS ===== */
section {
  max-width: var(--max-width);
  margin: 3rem auto;
  padding: var(--padding);
  background: #111;
  border: var(--border);
  border-radius: var(--radius);
  box-shadow: 0 0 20px rgba(255, 111, 0, 0.3);
  text-align: center;
}
section h2 {
  font-size: 2rem;
  margin-bottom: 1rem;
  color: var(--neon-yellow);
  text-shadow: 0 0 15px var(--neon-yellow);
}
section p {
  font-size: 1.1rem;
  color: var(--white);
}

/* ===== CTA / SIGNUP ===== */
.cta {
  background: rgba(255,111,0,0.1);
  border: var(--border);
  padding: 2rem;
  border-radius: var(--radius);
  transition: var(--transition);
}
.cta iframe {
  border: var(--border);
  border-radius: var(--radius);
  margin-top: 1rem;
}

/* ===== VIDEOS ===== */
.video-container iframe {
  border: var(--border);
  border-radius: var(--radius);
  max-width: 100%;
}

/* ===== FOOTER ===== */
footer {
  background: var(--black);
  color: var(--neon-yellow);
  text-align: center;
  padding: 2rem;
  border-top: var(--border);
}

/* ===== RESPONSIVE ===== */
@media(max-width:768px){
  header h1 { font-size: 2rem; }
  .hero h2 { font-size: 2rem; }
  section { margin: 2rem 1rem; }
  nav { flex-direction: column; gap: 1rem; }
}
</style>
</head>
<body>

<!-- HEADER -->
<header>
  <h1>Spark a Story</h1>
  <p>Helping kids bring their stories to life</p>
</header>

<!-- NAVIGATION -->
<nav>
  <a href="#about">About</a>
  <a href="#signup">Join Us</a>
  <a href="#videos">Videos</a>
</nav>

<!-- HERO -->
<section class="hero">
  <h2>Empower Young Voices</h2>
  <p>Workshops, resources, and community support to help kids turn ideas into powerful stories.</p>
</section>

<!-- ABOUT -->
<section id="about">
  <h2>Our Mission</h2>
  <p>
    At Spark a Story, we believe every young voice deserves to be heard.  
    Through workshops, online resources, and community support,  
    we help kids share their ideas and transform them into powerful stories.
  </p>
</section>

<!-- SIGNUP / CTA -->
<section id="signup" class="cta">
  <h2>Sign Up</h2>
  <p>Join our mailing list to hear about workshops and new resources!</p>
  <iframe 
    src="https://docs.google.com/forms/d/e/1FAIpQLSdu68mp17PfvZHtJxOMgl0DQeVHqv9IGKClO14IQdpN7ggBbQ/viewform?embedded=true" 
    width="640" 
    height="1633" 
    frameborder="0" 
    marginheight="0" 
    marginwidth="0">
    Loading…
  </iframe>
</section>

<!-- VIDEOS -->
<section id="videos">
  <h2>Our Videos</h2>
  <p>Check out our latest content on YouTube:</p>
  <div class="video-container">
    <iframe width="560" height="315"
      src="https://www.youtube.com/embed/dQw4w9WgXcQ"
      title="Spark a Story Intro Video"
      frameborder="0"
      allowfullscreen>
    </iframe>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <p>© 2025 Spark a Story | Built with ❤️ by young writers for young writers</p>
</footer>

</body>
</html>
